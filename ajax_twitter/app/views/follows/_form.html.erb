<!-- <%# if current_user.follows?(user) %>
  <%#= button_to 'Unfollow', user_follow_url(user), method: :delete %>
<%# else %>
  <%#= button_to 'Follow', user_follow_url(user), method: :post %>
<%# end %> -->

<button class="follow-toggle" data-user-id="<%=user.id%>"  data-initial-follow-state="<%= current_user.follows?(user) ? "followed" : "unfollowed"%>"></button>

<!-- <%#= button_to "", user_follow_url(user), method: current_user.follows?(user) ? :delete : :post, data: {"user-id" => "#{user.id}", "initial-follow-state" => "#{current_user.follows?(user) ? "followed" : "unfollowed"}"}, {class: 'follow-toggle'}%> -->
